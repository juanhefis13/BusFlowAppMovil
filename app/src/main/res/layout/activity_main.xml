<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/my_white"
    android:gravity="center"
    android:padding="16dp">

    <!-- Color Rojo en la parte superior -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#E91E63"
        android:orientation="vertical"
        android:padding="16dp"
        android:gravity="center">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Bienvenidos a BusFlow"
            android:textSize="24sp"
            android:textColor="#FFFFFF"/>
    </LinearLayout>

    <!-- Resto de la interfaz en color blanco -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="vertical"
        android:gravity="center"
        android:paddingTop="32dp"
        android:background="@color/my_white">

        <Button
            android:id="@+id/Conductor"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Conductor"
            android:textSize="18sp"
            android:drawableLeft="@drawable/ic_shortcut_driver_2"
            style="@style/ButtonStyle"
            android:layout_marginBottom="16dp"/>

        <Button
            android:id="@+id/Pasajero"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Pasajero"
            android:textSize="18sp"
            android:drawableLeft="@drawable/ic_shortcut_user"
            style="@style/ButtonStyle"/>
    </LinearLayout>
</LinearLayout>